const mongodb = require("mongodb");
import { Entity, model, property } from "@loopback/repository";
{{#each this}}{{#if isDatagrid}}import { {{dgPath}}BaseModel } from "./{{dgPath}}-model";{{/if}}{{#if isObject}}import { {{path}}BaseModel } from "./{{path}}-model";{{/if}}{{/each}}
@model({
  settings: {
    strictObjectIDCoercion: true
  }
})
export class {{_Model.name}}BaseModel extends Entity {
  {{> typeProperty}}
  
  constructor(data?: Partial< {{_Model.name}}BaseModel >) {
    super(data);
  }
}
# Example Task for Delphi Pipeline

Add a simple hello world endpoint to demonstrate the pipeline.

## Requirements
- Create a new TypeScript file called `hello.ts`
- Export a function that returns "Hello from Delphi!"
- Add proper TypeScript types
- Include a comment explaining the function
/**
 * Company-specific branding — replaced when Brain is forked for another company.
 *
 * Generic frontend code reads from here for any visual element that carries the
 * company's identity (logo, name, accent colour).
 *
 * This is the GENERIC EXAMPLE instance shipped with @goatlab/delphi-brain.
 * When you stand Brain up for a real company, replace this file (or have the
 * backend serve it from `brain.config.json` via `GET /api/config`).
 */
export const BRAND = {
  name: 'Delphi Brain',
  shortName: 'Brain',
  logoUrl: '/brain-logo.svg',
  tagline: 'Brain — company reasoning engine',
}
